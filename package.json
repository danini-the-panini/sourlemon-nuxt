{
  "name": "sourlemon",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "deploy": "yarn build && yarn generate && push-dir --dir=dist --branch=master --cleanup --remote=website"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "@nuxtjs/pwa": "^3.0.2",
    "nuxt": "^2.14.6",
    "push-dir": "^0.4.1",
    "spectre.css": "^0.5.9"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "sass-loader": "^10.0.2"
  }
}
