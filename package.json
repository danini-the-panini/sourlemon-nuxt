{
  "name": "sourlemon",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "deploy": "push-dir --dir=dist --branch=master --cleanup --remote=website"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "nuxt": "^2.14.6",
    "push-dir": "^0.4.1"
  }
}
